---
- name: Deploy OpenShift Origin
  hosts: all
  gather_facts: no
  vars:
    ansible_ssh_user: centos
    deployment_type: origin
    osm_cluster_network_cidr: 10.100.0.0/16
    osm_default_subdomain: apps.test.example.com
  include: playbooks/byo/config.yml
